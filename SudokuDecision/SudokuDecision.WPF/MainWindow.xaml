<Window x:Class="SudokuDecision.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SudokuDecision.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="300">
    <Window.Resources>
        <Style TargetType="DataGrid" x:Key="ColumnAllWidht">
            <Setter Property="ColumnWidth" Value="*"/>
        </Style>
    </Window.Resources>
    <Grid x:Name="aaa">

        <DataGrid x:Name="Table2" CanUserResizeColumns="False" Style="{StaticResource ResourceKey=ColumnAllWidht}" Margin="10,10,9.6,103.4"/>

        <Button x:Name="Zap" Content="Заполнить" Margin="0,0,9.6,68.4" HorizontalAlignment="Right" Width="81" Height="30" VerticalAlignment="Bottom" Click="Zap_Click"/>
    </Grid>
</Window>
